# BostonShell
A simple repo to hold my old C file simulating a shell program.

Bsh.c, which is a "shell" program (bsh stands for Boston shell). Intended to work like classic sh and bash. Programmed the following functionalities: env, setenv, unsetenv, cd, pwd and history.
